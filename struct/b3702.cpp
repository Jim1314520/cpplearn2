#include <iostream>
#include <vector>
using namespace std;
int main(){
  int m, n, sx, sy;
  cin >> m >> n >> sx >> sy;
  vector<vector< pair<int, int>>> vec(n, vector<pair<int, int>>(m));
  for(auto &line : vec){
    for(auto & [x,y] : line){
      cin >> x >> y;
    }
  }
  do{

  } while();
  
  

}