#include <iostream>
using namespace std;
#include <algorithm>
#include <vector>
int main(){
  int n;
  cin  >>n;
  vector<long long> coins(n);
  for(auto & x: coins) cin >> x;
  long long sum1 = 0;
  long long sum2 = 0;
  while(n--){

  }
}